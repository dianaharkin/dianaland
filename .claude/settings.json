{
  "//": "Pre-approved so Diana is never asked to judge a prompt she has no basis to judge. See CLAUDE.md rule 7.",
  "permissions": {
    "allow": [
      "Edit(*)",
      "Write(*)",
      "Read(*)",
      "Bash(git *)",
      "Bash(vercel *)",
      "Bash(curl *)",
      "Bash(sips *)",
      "Bash(cwebp *)",
      "mcp__*"
    ],
    "ask": [
      "Bash(rm *)",
      "Bash(git push --force*)",
      "Bash(git reset --hard*)"
    ],
    "deny": [
      "Read(./writing/corpus/**)",
      "Write(./.vercelignore)"
    ],
    "//deny": "The site work never needs to read her corpus, and it is no longer in this repo — it lives in her content engine. Both rules stay as guards against a stray copy: this repo is public, so anything landing in it is published immediately."
  }
}
